[project]
name = "dify-bsqb-plugin"
version = "0.1.0"
description = "Dify tool plugin for building Brave Search queries with bsqb"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "bsqb>=0.2.1",
    "dify_plugin>=0.9.0",
    "requests>=2.32.0",
]

[dependency-groups]
dev = []
